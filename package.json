{
  "name": "codemirror-atom-modes",
  "version": "1.1.1",
  "description": "Use Atom grammar files to apply syntax highlighting in a CodeMirror editor",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^3.5.0",
    "jshint": "^2.9.1",
    "mocha": "^2.4.5"
  },
  "dependencies": {
    "@pnidem/first-mate": "^6.2.1"
  }
}
